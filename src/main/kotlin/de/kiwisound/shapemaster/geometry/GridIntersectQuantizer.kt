package de.kiwisound.shapemaster.geometry

import kotlin.math.PI

class GridIntersectQuantizer {
    fun generateCircle(radius: Double, scale: Double) {
        var theta1 = 0.0
        var theta2 = 0.0
        while (theta1 < 2*PI && theta2 < 2*PI) {

        }
    }
}